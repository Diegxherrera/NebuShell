#include <stdio.h>

int main() {
    int exitSignal = 0;
    char currentCommand[] = "";
    while(exitSignal == 0) {
        printf("")
        switch (currentCommand) {
            case ""
        }
    }

    return 0;
}
